---
# settings for docker endpoint
# supported endpoints include http(s), tcp, unix socket and docker swarm!
docker:
  host: 192.168.99.101  # docker host ip, omit if using unix socket 192.168.99.100 | 172.23.121.59
  port: 2376  # docker reachable port, omit if using socket
  proto: https  # http | https | tcp | unix

buckets:
  default:
    count: 1
    ram: 256
    replica: 1
    type: couchbase

servers: # each server tag represents a cluster
 local:
    count: 4
    ram: 4096
    index_ram: 2048
    services: data,query,index
    rest_username: Administrator
    rest_password: password
    rest_port: 8091
    query_port: 8093
    init_nodes: 4
    buckets: default